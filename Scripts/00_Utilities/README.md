# Utilities

Small bookkeeping helpers used while checking sample/file lists. `compare_strain_lists.py` performs light normalization of common filename suffixes and a few known historical strain-name variants.
